# Legacy authentication design

Status: superseded

The first browser client stored JWT access tokens in `localStorage` and sent
them in an `Authorization` header. This document remains only as migration
history and must not guide new authentication work.

